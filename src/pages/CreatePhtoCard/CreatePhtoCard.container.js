import { connect } from 'react-redux';
import CreatePhtoCardScreen from './CreatePhtoCard.component';
 
  
const CreatePhtoCardScreenContainer = connect()(CreatePhtoCardScreen);

export default CreatePhtoCardScreenContainer;
