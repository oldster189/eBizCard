import { connect } from 'react-redux';
import CreateProfileScreen from './CreateProfile.component';
 
  
const CreateProfileScreenContainer = connect()(CreateProfileScreen);

export default CreateProfileScreenContainer;
