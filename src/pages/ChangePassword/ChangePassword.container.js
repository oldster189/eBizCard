import { connect } from 'react-redux';
import ChangePasswordScreen from './ChangePassword.component';
 
  
const ChangePasswordScreenContainer = connect()(ChangePasswordScreen);

export default ChangePasswordScreenContainer;
