import { connect } from 'react-redux';
import ResetPasswordScreen from './ResetPassword.component';
 
  
const ResetPasswordScreenContainer = connect()(ResetPasswordScreen);

export default ResetPasswordScreenContainer;
